[project]
name = "photo-ai-factory-worker"
version = "0.1.0"
requires-python = ">=3.11,<3.14"
dependencies = [
  "fastapi>=0.115",
  "uvicorn>=0.30",
  "pydantic>=2.8",
  "numpy>=1.26",
  "opencv-python-headless>=4.10"
]

[project.optional-dependencies]
test = ["pytest>=8", "httpx>=0.27"]

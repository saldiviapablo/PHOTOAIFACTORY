<Page
    x:Class="PhotoAIFactory.App.Pages.ReviewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:PhotoAIFactory.Application.UI"
    mc:Ignorable="d">

    <Grid RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Spacing="4">
                <TextBlock Text="Review Station" Style="{ThemeResource TitleTextBlockStyle}"/>
                <TextBlock Text="Manual quality review and publication decisions for flagged photos." Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>

            <Button Grid.Column="1" Content="Refresh" Command="{x:Bind ViewModel.RefreshCommand}"/>
        </Grid>

        <!-- Two Column Review Layout -->
        <Grid Grid.Row="1" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Review Items List -->
            <ListView
                ItemsSource="{x:Bind ViewModel.PendingReviews, Mode=OneWay}"
                SelectedItem="{x:Bind ViewModel.SelectedItem, Mode=TwoWay}"
                SelectionMode="Single">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="ui:ReviewItemDto">
                        <Grid Padding="12" Margin="0,4" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                            <StackPanel Spacing="4">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="{x:Bind PhotoName}" FontWeight="SemiBold"/>
                                    <Border Background="{ThemeResource SystemFillColorNeutralBackgroundBrush}" CornerRadius="4" Padding="6,2">
                                        <TextBlock Text="{x:Bind ReviewStage}" FontSize="10"/>
                                    </Border>
                                </StackPanel>
                                <TextBlock Text="{x:Bind JobState}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="{x:Bind CreatedAtUtc}" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <!-- Right: Selected Review Details & Action Panel -->
            <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="20">
                <Grid>
                    <TextBlock Text="Select a photo from the review list to inspect findings and take action."
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               Visibility="{x:Bind ViewModel.SelectedItem, Mode=OneWay, Converter={StaticResource NullToVis}, ConverterParameter='Invert'}"/>

                    <StackPanel Spacing="16" Visibility="{x:Bind ViewModel.SelectedItem, Mode=OneWay, Converter={StaticResource NullToVis}}">
                        <TextBlock Text="{x:Bind ViewModel.SelectedItem.PhotoName, Mode=OneWay, FallbackValue=''}" Style="{ThemeResource SubtitleTextBlockStyle}"/>

                        <!-- Status Message -->
                        <TextBlock Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" Foreground="{ThemeResource SystemFillColorSuccessBrush}" FontWeight="Medium"
                                   Visibility="{x:Bind ViewModel.StatusMessage, Mode=OneWay, Converter={StaticResource NullToVis}}"/>

                        <!-- Findings Summary -->
                        <Border Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="6" Padding="12">
                            <StackPanel Spacing="6">
                                <TextBlock Text="QA Findings" FontWeight="SemiBold"/>
                                <TextBlock Text="{x:Bind ViewModel.SelectedItem.Findings, Mode=OneWay, FallbackValue='None'}" TextWrapping="Wrap" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            </StackPanel>
                        </Border>

                        <!-- Action Buttons -->
                        <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,16,0,0">
                            <Button Style="{ThemeResource AccentButtonStyle}" Content="{x:Bind ViewModel.ApproveButtonLabel, Mode=OneWay}" Command="{x:Bind ViewModel.ApproveCommand}"/>
                            <Button Content="Reprocess (Quality)" Command="{x:Bind ViewModel.ReprocessCommand}"/>
                            <Button Content="Reject" Command="{x:Bind ViewModel.RejectCommand}"/>
                            <Button Content="Leave Pending" Command="{x:Bind ViewModel.LeavePendingCommand}"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>
